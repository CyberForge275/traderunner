"""Typed config mapping for harami_break strategy."""

from dataclasses import dataclass


@dataclass(frozen=True)
class HaramiBreakConfig:
    """Placeholder config for future implementation."""

    strategy_version: str
